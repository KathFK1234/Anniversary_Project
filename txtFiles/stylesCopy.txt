/* Body and background color */
body {
  font-family: 'Arial', sans-serif;
  background-color: #D0F0C0;
  text-align: center;
  margin: 100px;
  padding: 0;
  height: 700vh;
  transition: all 2.5s ease;
}

/* Hide the audio element so it doesn’t appear on the page */
audio {
  display: none;
}

/* Smooth Scrolling */
html {
  scroll-behavior: smooth;
}

/* Quiz form styling */
.quiz-container {
  background-color: whitesmoke;
  padding: 3rem;
  border-radius: 15px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  margin-top: 50px;
  margin-bottom: 50px;
  max-width: 400px;
}

.quiz-container h1 {
  text-align: center;
  font-size: 24px;
  margin-top: -10px;
  margin-bottom: 20px;
}

.question {
  margin-bottom: 20px;
}

.question input {
  text-align: center;
  padding: 10px;
  font-size: 16px;
  width: 100%;
  border-radius: 5px;
  border: 1px solid #ddd;
}

/* Slideshow styling */
.slideshow {
  display: none;
  position: relative;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: -1;
  opacity: 0;
  transform: translateY(100px); /* Start below viewport */
  transition: opacity 1s ease, transform 1s ease; /* Smooth reveal */
}

.slideshow.show {
  opacity: 1;
  transform: translateY(0); /* Bring to normal position */
}

.description {
  /*position: relative;*/
  bottom: 30px; /* Position it near the bottom of the image */
  left: 50%; /* Start with the text in the center */
  transform: translateX(-50%); /* Move it back to exact center */
  /*color: white; */
  font-size: 20px;
  /* background: rgba(0, 0, 0, 0.5); /* Semi-transparent background for readability */
  
  width: 500px;
  /*text-align: center;*/
}

/*.description {
  position: absolute;
  bottom: 280px; /* You can adjust this to move the text higher or lower
  left: 30%;
  transform: translateX(-50%); /* Center the paragraph horizontally
  font-size: 25px;
}

.slide {
  position: absolute;
  width: 80%;
  height: 80%;
  opacity: 0;
  transition: all 1s ease-in-out;
}

.slide img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 10px;
}

.slide.show {
  opacity: 1;
}*/

/* Overlapping images effect (like Apple product pages)
img {
  position: relative;
  z-index: 1;
  margin-left: -40px; /* Adjust to overlap 
}*/